diff --git a/AGENT-REVIEW.md b/AGENT-REVIEW.md
index 7796d39..2355aeb 100644
--- a/AGENT-REVIEW.md
+++ b/AGENT-REVIEW.md
@@ -78,6 +78,24 @@
 
 两个 Agent 同时检查、同时认领可能造成轻微超额（> MAX）。按 2.5-3 处理：后到者（认领评论时间戳较晚）退出。**任何 Agent 都不应**在已满后仍继续认领。
 
+### 2.7 请求再次审核（作者侧触发动作）
+
+**适用对象**：PR 作者（Agent）。审核方提交 REQUEST_CHANGES（或带修改意见的 COMMENT / 打回评论）后，作者完成修复、推送新 commit 时：
+
+1. **必须在同一 PR 的评论中发一条「请求再次审核」**，作为触发审核方复测的显式动作。格式（第一行）：
+
+   ```
+   [AGENT-REVIEW] 请求再次审核 #<N>：已按 REQUEST_CHANGES 修复，commit <sha>
+   ```
+
+   前缀 `[AGENT-REVIEW]` 沿用认领标记（机器可解析），正文需包含「请求再次审核」字样、PR 编号、修复 commit 的 sha（便于审核方定位增量）。
+
+2. **审核方以该评论作为复测触发信号**：轮询/scan 发现作者推了新 commit 时，**只有当作者同时发了「请求再次审核」评论才对该轮修复执行复测**；只推 commit、未发请求评论 = 作者尚未声明修复完成（可能仍在演进），审核方不做复测，避免对中间态 commit 空转。
+
+3. **每响应一轮 REQUEST_CHANGES 都要重复此动作**：作者修复 → 发请求再次审核 → 审核方复测 → 新一轮 REQUEST_CHANGES → 再修复再请求，循环直至 APPROVE / 维护者处置。收到 APPROVE 后不再需要请求。
+
+4. 维护者侧（或其 Agent）可在收到「请求再次审核」评论后，将该作者的修复 commit 纳入复测队列（与 AGENTS.md / DEVELOPMENT.md §2-14 同步生效）。
+
 ## 3. 审核要求（认领后必须遵守）
 
 1. **时限**：认领后 **24 小时内**提交审核结论（review 或 [AGENT-REVIEW-DONE] 评论）。超时未完成 = 认领失效，其他 Agent 可补位。
diff --git a/DEVELOPMENT.md b/DEVELOPMENT.md
index 2204266..a37c146 100644
--- a/DEVELOPMENT.md
+++ b/DEVELOPMENT.md
@@ -33,6 +33,7 @@ PR 由 AI Agent 编写提交（人类只提出需求、不直接编码）。以
 11. **验证说明**：PR 描述必须写明「需求来源 → 实现方式 → 验证过程与结果」三段式说明（PR 的审查方也可能是 AI Agent，需要可复现的验证信息）。可参考 `test-apis.mjs` / `test-websocket.mjs` / `test-ws-direct.mjs`。
 12. **平台专属代码（如 Windows 命名管道）必须满足**：平台门控（非目标平台直接禁用）、探测失败静默回退到跨平台路径、封装 `core/` 模块与跨平台路径共用同一入口、文档中标注适用平台与回退行为。禁止以「平台专属」为由绕过第 2 条（无个人环境硬编码）与第 5 条（文档同步）。
 13. **以 AI Agent 口吻提交**：commit 信息、PR 描述、issue 正文一律用 Agent 自身口吻书写（见 §1「身份表达」），不冒用背后使用者的人称（署名可以用使用者账号，但口吻必须是 Agent）；需求来源段落（第 11 条三段式说明中的"需求来源"）写"使用者提出…"而非"我需要…"，让作者与审查方清楚区分 Agent 的陈述与使用者的原始需求。
+14. **响应审核后必须请求再次审核**：收到审核方 REQUEST_CHANGES（或带修改意见的 COMMENT / 打回评论）并完成修复、推送新 commit 后，**必须在同一 PR 的评论中发一条「请求再次审核」**（标记格式见 AGENT-REVIEW.md §2.7），作为触发审核方复测的动作。**只推 commit 不发请求评论 = 视为未响应审核**，审核方不会对该轮修复执行复测；每响应一轮 REQUEST_CHANGES 都要重复此动作（收到 APPROVE 后不再需要）。
 
 > 目前仓库没有 CI，上述脚本是手动验证工具。合并决策由作者（或其 AI Agent）实际运行验证后作出。
 
