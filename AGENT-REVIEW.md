# 协作审核协议（Collaborative Review Protocol）

> 本文档定义 vrchat-assistant 仓库的**分布式协作审核机制**：多个独立部署的 AI Agent（各自服务不同的使用者）可以共同参与 PR / issue 的审核，通过**认领评论**声明参与，满员后不再重复劳动。任何 Agent（含人类）阅读本文档即可参与，无需向仓库维护者申请权限。

## 1. 参与资格（Opt-in）

1. Agent 在**首次安装 / 部署本项目时**（配置阶段，遵循 AGENTS.md「安装前检查」流程），必须询问其背后使用者："是否愿意参与 vrchat-assistant 的 PR/issue 协作审核？"
   - 使用者同意 → 配置定时检查（参考脚本 + 定时任务），开始参与；
   - 使用者拒绝或未答复 → **不得参与**。
   - 若安装时未确认（如无交互环境），Agent 不得自行假设同意；应在后续首次有机会与使用者交互时补问，确认前不参与。
2. Agent 需要：
   - 一个 GitHub 账号（评论以该账号名义发出）；
   - 能调用 GitHub REST API（`gh api` 或等价手段）；
   - 能读取本仓库（公开仓库，无需 clone 即可审核，但 clone 到本地实测更可靠）。
3. 所有认领评论与审核文字一律以 **Agent 自身口吻**书写（遵守 DEVELOPMENT.md §1「身份表达」：署名可用使用者账号，文字不冒用人类第一人称）。

## 2. 认领协议（Claim）

### 2.1 认领评论格式

在目标 PR / issue 上发一条 issue comment，**第一行必须**以下面前缀开头（前缀之后可自由补充）：

```
[AGENT-REVIEW] 认领 #<N> 审查（agent: <你的GitHub登录名>）
```

- 前缀 `[AGENT-REVIEW]` 是机器可解析的固定标记，正则：`^\[AGENT-REVIEW\]`。
- `<N>` 是 PR 或 issue 的编号（认领评论所在条目本身）。
- `agent:` 后必须写明认领者的 GitHub 登录名，便于其他 Agent 去重识别。

### 2.2 完成声明

审核完成后，在同一条目发一条完成评论：

```
[AGENT-REVIEW-DONE] #<N> 审核完成：<一句话结论（如 APPROVE / REQUEST_CHANGES / COMMENT + 要点）>
```

- PR 场景下，提交过正式 review（`pulls/N/reviews` 中 APPROVE / REQUEST_CHANGES / COMMENT，非 PENDING）也视为完成，`[AGENT-REVIEW-DONE]` 可省略。
- issue 场景下，必须发 `[AGENT-REVIEW-DONE]` 标记完成。

### 2.3 参与人数（有效认领）计数

对目标条目，取全部 issue comments（`issues/N/comments`），有效认领按以下规则统计：

1. 匹配认领前缀 `[AGENT-REVIEW]` 的评论；
2. **按评论作者（`user.login`）去重**（同一 Agent 重复认领只算 1 人）；
3. **排除条目作者本人**（作者自己认领自己的条目不计，作者发言不影响计数）；
4. **排除已退出的认领**：作者在同一条目后发过 `[AGENT-REVIEW-WITHDRAW]` 则其认领作废；
5. **排除失效认领**：认领评论发出超过 24 小时、且该作者在该条目**没有完成证据**（无 `[AGENT-REVIEW-DONE]`，PR 场景无正式 review）→ 视为放弃，不计入。

**有效认领数 = 已承诺参与该条目的 Agent 数（含已完成者）**。认领后完成审核 → 名额**不释放**，仍计入有效认领——满员后该条目不再接收新参与者，参与人数稳定；只有**退出**或**失效**才让出名额。

### 2.4 满员阈值

- 默认 **MAX_REVIEWERS = 3**（可配置，以本协议为准，仓库维护者可调整）。
- 有效认领数 **≥ MAX_REVIEWERS** 时，该条目视为"已满"，其他 Agent 不做处理（可在简报中标注已满，但不认领、不审核）。**已完成审核的认领仍占名额**（完成不释放），避免一条条目被无限追加参与者。
- **重要关联豁免**：若 Agent 判断该 PR/issue 与自身或背后使用者有**重要关联**，则**不受满员限制**，仍可认领并参与审核。重要关联的典型情形：
  - 使用者是该 issue 的提出者，或该 PR 修复的 bug / 需求的来源（使用者提的需求被实现）；
  - 该 PR/issue 直接涉及使用者的使用场景（使用者依赖的功能、使用者报告的问题、使用者在该条目中被 @ 或点名）；
  - Agent 掌握其他参与者没有的第一手信息（需求背景、复现环境、使用者预期）。
  - **判定权在认领 Agent 自身**（诚实自评，勿滥用）；豁免认领的评论必须注明关联理由（如「重要关联：使用者为此 issue 提出者」）。

### 2.5 认领流程（参与方 Agent 执行）

```
1. 定时检查（建议每 30-60 分钟）open PR + open issues（排除 draft PR、已关闭/已合并）
2. 对每条候选条目：
   a. 若自己已认领（自己发过 [AGENT-REVIEW]）→ 跳过（继续处理中或检查是否该完成声明）
   b. 计算有效认领数
   c. 有效认领数 >= MAX → 跳过；**若与自身/使用者有重要关联（§2.4 豁免），不跳过，认领并注明理由**
   d. 否则 → 发认领评论 → 立即重新计数
3. 认领后若重数发现自己超员（并发竞态导致 > MAX）→ 发 [AGENT-REVIEW-WITHDRAW] 退出，不再审核；**§2.4 重要关联豁免的认领不因超员退出**（豁免即允许超过满员上限）
4. 认领成功后执行审核（见 §3），完成后发 [AGENT-REVIEW-DONE]（或提交正式 review）
```

### 2.6 并发竞态

两个 Agent 同时检查、同时认领可能造成轻微超额（> MAX）。按 2.5-3 处理：后到者（认领评论时间戳较晚）退出。**任何 Agent 都不应**在已满后仍继续认领。

### 2.7 请求再次审核（作者侧触发动作）

**适用对象**：PR 作者（Agent）。审核方提交 REQUEST_CHANGES（或带修改意见的 COMMENT / 打回评论）后，作者完成修复、推送新 commit 时：

1. **必须在同一 PR 的评论中发一条「请求再次审核」**，作为触发审核方复测的显式动作。格式（第一行）：

   ```
   [AGENT-REVIEW] 请求再次审核 #<N>：已按 REQUEST_CHANGES 修复，commit <sha>
   ```

   前缀 `[AGENT-REVIEW]` 沿用认领标记（机器可解析），正文需包含「请求再次审核」字样、PR 编号、修复 commit 的 sha（便于审核方定位增量）。

2. **审核方以该评论作为复测触发信号**：轮询/scan 发现作者推了新 commit 时，**只有当作者同时发了「请求再次审核」评论才对该轮修复执行复测**；只推 commit、未发请求评论 = 作者尚未声明修复完成（可能仍在演进），审核方不做复测，避免对中间态 commit 空转。

3. **每响应一轮 REQUEST_CHANGES 都要重复此动作**：作者修复 → 发请求再次审核 → 审核方复测 → 新一轮 REQUEST_CHANGES → 再修复再请求，循环直至 APPROVE / 维护者处置。收到 APPROVE 后不再需要请求。

4. 维护者侧（或其 Agent）可在收到「请求再次审核」评论后，将该作者的修复 commit 纳入复测队列（与 AGENTS.md / DEVELOPMENT.md §2-14 同步生效）。

## 3. 审核要求（认领后必须遵守）

1. **时限**：认领后 **24 小时内**提交审核结论（review 或 [AGENT-REVIEW-DONE] 评论）。超时未完成 = 认领失效，其他 Agent 可补位。
2. **PR 审核最低标准**：
   - 提交 GitHub review（APPROVE / REQUEST_CHANGES / COMMENT）+ 必要的 inline comments（行号必须在 diff hunk 内）；
   - 阻断项（🔴）必须附**可复现的实测证据**（报错输出 / 运行结果），不只讲理论；
   - 遵守仓库 DEVELOPMENT.md §2 全部要求（单一职责、无个人环境硬编码、DB 迁移、文档同步等）；
   - 只读审核：**不得直接 push 到主仓库分支**；不得修改 PR 作者分支（除非维护者明确授权）。
3. **issue 审核最低标准**：给出结论评论，含可复现的证据（复现步骤 / 数据 / 代码定位到函数级）。
4. **身份与口吻**：全部文字用 Agent 口吻；署名可用背后使用者的 GitHub 账号。
5. **合并权**：协作审核只产出**审核意见**。合并 / 关闭 / 打回决策权在仓库维护者（ggg123124 侧），参与审核的 Agent **不得自行合并**。维护者（或其自己的 Agent）会参考多方 review 意见后做最终处置。
6. **审核视角（个性化）**：每个参与 Agent 的审核方式与侧重点由**自身环境**与**背后使用者的习惯**决定，**无需一致**：
   - **运行环境**：本机是否有仓库 clone、能否执行测试、gh 权限等级（能否提交正式 review）、网络条件——决定审核深度（端到端实测 / 静态审查 / 评论级 review）。环境受限时按本条第 2 项明示验证范围，证据标准不降低。
   - **使用者视角**：审核时站在其背后使用者的立场，额外输出「使用者视角」评估——该改动对使用者关注的平台、使用习惯、数据与体验的影响。不同使用者的 Agent 意见可以不同甚至冲突，这正是协作审核的价值：维护者综合多方立场判断。
   - **表达方式**：公开的 review 意见遵守本协议（Agent 口吻、可复现证据、分级反馈、认领/完成标记）；Agent 与背后使用者的沟通方式（汇报形式、语言风格）按双方既有习惯，不受本协议约束。
   - **视角声明**：认领后的 review（或 `[AGENT-REVIEW-DONE]` 评论）开头注明「审核环境：<实测 / 静态 / 评论级>；使用者视角：<一句话>」，便于维护者理解该意见的立场与深度。

### 3.7 索引 PR 简化审核（1 名审核人即可）

**适用对象**：**索引 PR**——只改动 `plugins/index.json`（社区插件索引）的 PR，不涉及任何核心/官方插件/工具定义的代码变更。

- **阈值减半到 1 人**：索引 PR 的有效认领数达到 **1** 即视为"已满"，其他 Agent 不再重复认领（普通代码 PR 维持默认 `MAX_REVIEWERS = 3`）。只改一个非运行时索引文件，风险低、无需多人复测。
- **审核范围收敛**：认领者重点核对 ① 条目四项齐全（`name` / `git` / `description` / `maintainer`）；② `name` 在 `core/tool-order.json` 与全仓库工具命名空间中唯一；③ PR 描述已注明插件来源仓库 URL、维护者、一句话描述、以及「插件可在本地 `plugins/local/` 运行且不破坏现有 MCP 工具」的验证说明；④ 未夹带任何敏感信息。不要求完整端到端实测。
- **仍应快速抽验**：即使无需 3 人，认领的 Agent 也应确认 `node test-registry.mjs` 与 `python scripts/check-doc-drift.py --json` 的 `has_drift=false` 未被破坏（索引文件不参与注册表，正常不会影响，但抽验可防夹带）。
- **判定权不变**：简化只影响**参与人数与深度**，合并/关闭/打回决策权仍归仓库维护者；审核仍须遵守 §3 的「阻断项须附可复现证据」「只读、不得直接 push」等总则。

## 4. 参考脚本

仓库提供参考实现：`scripts/agent-review.py`（Python 3，依赖 `gh` CLI，REST-only）。

```
usage: agent-review.py <子命令> [选项]

status [--repo O/R] [--max N] [--detail]
    列出所有 open PR + issue 及认领状态（有效认领数 / 是否已满 / 参与者）

scan [--repo O/R] [--state <path>]
    变化监测：对比状态文件输出「新增 / 有更新 / 已消失」的条目（供定时任务驱动）

claim <number> [--repo O/R] [--user <login>] [--max N]
    认领条目（自动判断 PR/issue）。draft / 已满 / 自己已认领 / 作者本人 → 拒绝并说明
    认领成功后自动重数，超员则提示并建议 withdraw

withdraw <number> [--repo O/R] [--user <login>]
    退出认领（发 [AGENT-REVIEW-WITHDRAW] 评论）

mine <number> [--repo O/R] [--user <login>]
    查询自己是否已认领 / 是否已完成
```

脚本实现约定：
- **一律用 `gh api` REST**（本仓库 `gh pr list` 等 GraphQL 命令会报 `Could not resolve to a Repository`）；
- 计数严格按 §2.3 规则；
- 认领评论正文第一行严格为 `[AGENT-REVIEW] 认领 #<N> 审查（agent: <login>）`；
- 网络 / API 不可用时优雅降级（不误报、不改状态）。

## 5. 边界与 FAQ

- **Q: 认领了但审不完怎么办？** 发 `[AGENT-REVIEW-WITHDRAW]` 退出，或让 24h 超时自动失效。
- **Q: 所有 PR 都需要多人审吗？** 不。单 Agent 认领即可开始；多人是"能力复用"而非"强制门槛"。维护者可合并**任何**审核意见充分、实测通过的 PR，与认领人数无关。
- **Q: 认领需要权限吗？** 不需要。认领 = 发评论（公开仓库任何人可发）。正式 review 需要仓库写入权限；无权限的 Agent 用 COMMENT 级 review 或结论评论代替（仍按 §2.2 标记完成）。
- **Q: 如何避免刷屏？** 每条目最多 MAX_REVIEWERS 个认领 + 退出/完成标记，Agent 认领前先数再发。
- **Q: 个人化数据？** 本协议不涉及任何数据落库；状态文件（scan 用）为各参与方本地文件，不进仓库。

---

*协议版本: v1.0（2026-08-18）。仓库维护者对本协议有最终解释权。*
